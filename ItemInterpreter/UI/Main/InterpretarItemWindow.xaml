<Window x:Class="ItemInterpreter.UI.Main.InterpretarItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Interpretar Item" Height="600" Width="800"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🧾 Colar item hexadecimal abaixo (32 bytes)" FontSize="18" FontWeight="Bold" />

        <TextBox x:Name="HexInputTextBox" Grid.Row="1" Margin="0 10 0 10"
                 FontFamily="Consolas" FontSize="14" Height="100"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="2">
            <Button Content="🧪 Interpretar" Click="InterpretItem_Click"
                    Height="40" Width="150" HorizontalAlignment="Left"
                    Background="#007ACC" Foreground="White" FontSize="14" Margin="0 0 0 10"/>

            <ScrollViewer Height="350" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ResultTextBlock" FontFamily="Consolas" FontSize="14" TextWrapping="Wrap"/>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Window>
